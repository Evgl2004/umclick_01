# Дорожная карта umclick

Актуально на 16 мая 2026.

## Где мы сейчас

Мы на этапе расширенного MVP. Базовый сценарий уже собран: преподаватель может создать викторину, запустить live-сессию, показать QR/ссылку входа, участники могут быстро зарегистрироваться и отвечать на вопросы, результаты считаются и экспортируются.

Архитектурный UI-распил Flutter-части доведен до разумной точки остановки: `main.dart` отвечает за вход и навигацию, участник и преподаватель живут в feature-модулях, крупные карточки преподавателя/участника вынесены в отдельные виджеты. Дальше новые выносы делаем только при явной боли: дублирование, сложный state-flow, тестируемость или заметный рост файла.

## Уже сделано

- Базовая инфраструктура проекта: Django + DRF, Flutter Web, PostgreSQL, Redis, Celery, Docker Compose.
- Ветки `main` и `develop-cai`, разработка ведется в `develop-cai`.
- Регистрация и JWT-вход преподавателя.
- CRUD викторин для преподавателя.
- Визуальный конструктор викторин во Flutter.
- Создание live-сессий с PIN, QR и `join_token`.
- Подключение участника по PIN или по ссылке `/join?token=...`.
- Предпросмотр сессии до регистрации участника.
- Быстрая регистрация участника: телефон, имя, согласие.
- Версионирование согласий и публичные legal-страницы.
- WebSocket-события для live-сессий.
- Управление раундом: старт, следующий вопрос, раскрытие ответов, завершение.
- Таймер вопроса и авто-раскрытие через Redis + Celery beat.
- Kahoot-style скоринг по скорости ответа.
- Leaderboard и CSV-экспорт результатов.
- Первый слой локализации интерфейса: русский по умолчанию, переключатель RU/EN, сохранение выбора языка.
- Инфраструктура языка и первый каталог строк вынесены во Flutter-модуль `frontend/lib/l10n`.
- Основные, динамические и legal-строки интерфейса вынесены из `main.dart` в каталог переводов.
- API-клиент и базовые helper-функции вынесены из `main.dart` в отдельные модули `frontend/lib/api` и `frontend/lib/core`.
- Конфигурационные константы и модели черновика викторины вынесены в отдельные frontend-модули.
- Панель преподавателя вынесена из `main.dart` в отдельный feature-модуль `frontend/lib/features/teacher/teacher_panel.dart`.
- Экран участника, карточка вопроса и legal-страницы вынесены из `main.dart` в feature-модули `frontend/lib/features/participant` и `frontend/lib/features/legal`.
- Начат общий UI-слой `frontend/lib/shared/widgets`: вынесены базовая карточка секции и статусная плашка.
- Карточка вопроса участника вынесена в отдельный feature-widget `frontend/lib/features/participant/widgets/question_card.dart`.
- Разбор join-ссылки участника (`api`, `token`, `pin`) вынесен в тестируемую модель `frontend/lib/features/participant/models/join_source.dart`.
- Форматирование и ограничение live-событий вынесены в общий helper `frontend/lib/core/live_event_log.dart` для участника и преподавателя.
- Общая логика countdown-таймера вынесена в `frontend/lib/core/countdown_ticker.dart` и подключена к экранам участника/преподавателя.
- Техническое WebSocket-подключение и JSON-парсинг вынесены в `frontend/lib/core/live_socket_connection.dart`, обработка событий осталась в feature-экранах.
- Live UI-блоки участника вынесены в `frontend/lib/features/participant/widgets/live_session_widgets.dart`.
- Hero-блок участника вынесен в `frontend/lib/features/participant/widgets/participant_hero.dart`.
- Карточка подключения участника и preview сессии вынесены в `frontend/lib/features/participant/widgets/join_connection_card.dart`.
- Карточка профиля/согласия участника вынесена в `frontend/lib/features/participant/widgets/profile_card.dart`.
- Хранение auth-сессии преподавателя вынесено из панели в `frontend/lib/features/teacher/teacher_auth_session.dart`.
- Загрузка, создание и API-payload черновика викторины вынесены в `frontend/lib/features/teacher/quiz_draft_mapper.dart`.
- Карточка авторизации преподавателя вынесена в `frontend/lib/features/teacher/widgets/teacher_auth_card.dart`.
- Карточка настройки live-сессии преподавателя вынесена в `frontend/lib/features/teacher/widgets/teacher_session_setup_card.dart`.
- Верхняя часть live-сессии преподавателя с PIN/QR/статусами вынесена в `frontend/lib/features/teacher/widgets/teacher_live_session_header.dart`.
- Кнопки управления live-раундом преподавателя вынесены в `frontend/lib/features/teacher/widgets/teacher_round_controls.dart`.
- Карточка результатов раскрытого ответа преподавателя вынесена в `frontend/lib/features/teacher/widgets/teacher_reveal_results_card.dart`.
- Карточка live-событий преподавателя вынесена в `frontend/lib/features/teacher/widgets/teacher_live_events_card.dart`.
- Карточка редактирования вопроса викторины вынесена в `frontend/lib/features/teacher/widgets/teacher_quiz_question_card.dart`.
- Карточка конструктора викторины преподавателя вынесена в `frontend/lib/features/teacher/widgets/teacher_quiz_builder_card.dart`.
- Оболочка live-сессии преподавателя вынесена в `frontend/lib/features/teacher/widgets/teacher_live_session_card.dart`.
- Первые пользовательские подсказки в интерфейсе.
- Начат Kahoot-like редизайн экрана участника: игровой hero-блок, карточки подключения/регистрации, live-блок раунда и крупные цветные варианты ответов.
- Начат редизайн панели преподавателя под live-ведение: отдельный блок сессии, PIN/QR, метрики, управление раундом и события.

## Что важно улучшить дальше

- Полноценная дизайн-система в стиле игрового квиза: довести экран участника, добавить динамику раунда и унифицировать визуальный язык преподавателя.
- Полная локализация всех сообщений, включая ошибки API и live-события.
- Переход с текущего каталога строк на полноценную Flutter `l10n`/ARB-схему.
- Дальнейшее разделение frontend-модулей делаем точечно, только если появляется явная причина: дублирование, сложная логика состояния, тестируемость или рост конкретного файла.
- Тесты backend API: join по token/PIN, preview, ответы, таймер, экспорт.
- Frontend smoke-тесты ключевых сценариев.
- Улучшение безопасности персональных данных: политика хранения, удаление/анонимизация, аудит доступа.
- Роли и управление преподавателями.
- Импорт/экспорт викторин.
- Более удобная аналитика по результатам сессии.

## Ближайшие шаги

1. Настроить доступный запуск Flutter/Dart в окружении или CI, чтобы `dart format` и `flutter analyze` стали обязательной проверкой.
2. Провести визуальную smoke-проверку Flutter Web: преподаватель создает викторину, запускает сессию, участник подключается и отвечает.
3. Добавить backend-тесты на публичные endpoints и join-flow: preview, join по token/PIN, ответы, таймер, раскрытие, экспорт.
4. Добавить frontend smoke-тест ключевого сценария через контейнер или CI.
5. После проверок вернуться к продуктовым улучшениям: дизайн-система, полная локализация, безопасность персональных данных, аналитика результатов.

## Принципы разработки

- Маленькие, проверяемые изменения.
- Отдельный коммит на логически завершенный шаг.
- Русские сообщения коммитов.
- Сначала сохраняем рабочий MVP, затем улучшаем архитектуру.
- Изменения в UI делаем вместе с понятными состояниями: загрузка, ошибка, пустой список, успешный результат.
- Все, что касается персональных данных, делаем явно и версионируемо.
